import React, {FunctionComponent} from 'react';
import styles from "./LoginPage.module.css";

type LoginPageProps = {
    id: string,
    name: string
};

/***
 * LoginPage component.
 * @param id -
 * @param name -
 * returns a LoginPage with :TODO
 */

const LoginPage: FunctionComponent<LoginPageProps> = ({id, name}) => {
    return (<div>
    </div>);
};

export default LoginPage;